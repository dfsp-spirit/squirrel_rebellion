# squirrel_rebellion
Messing with the [phaser](https://phaser.io/) JavaScript game engine for fun and squirrels


## Run the dev version

You will need `npm` installed. In the repo root, after cloning:

```shell
cd squirrel-rebellion/
npm install  # if you haven't already
npm run dev
```

